# Зависимости личного ассистента (Python)
aiogram>=3.7,<4        # Telegram-бот
anthropic>=0.40        # клиент Claude API
python-dotenv>=1.0     # чтение .env
PyYAML>=6.0            # конфиги агентов
telethon>=1.36         # Telegram Client API (MTProto) — сбор истории канала
openpyxl>=3.1          # выгрузка таблицы в Excel
qrcode>=7.4            # QR-вход в Telegram без кода по номеру (login.py)

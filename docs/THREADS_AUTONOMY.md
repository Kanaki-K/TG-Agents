# 🧵 Автономная петля Threads — целевая схема (спец владельца 09.09.2026)

> Как это должно работать в итоге, его словами: «Я нажимаю одну кнопку и по графику получаю посты в
> ТГ + переработанные посты в Threads. Они сделаны заранее, за 4 часа до публикации — чтобы админ мог
> просмотреть, отредактировать, проверить. Если изменений не было — публикует по графику».
>
> Документ описывает ЦЕЛЬ и порядок сборки. Что уже стоит на месте — в [threads.md](threads.md) §11
> и [ARCHITECTURE.md](ARCHITECTURE.md) §4.1/§4.2.

## 1. Полный цикл одного дня

```
16:00−4ч   автопилот гонит ТГ-формат (флагман Вт/Чт | скоуп Пн/Ср/Пт)
           → пост + обложка ложатся в ОТЛОЖКУ боевого канала
           → владельцу уведомление на мейн
           → владелец правит ПРЯМО В ОТЛОЖКЕ (ТГ опубликует ровно то, что там осталось)
16:00      Telegram публикует пост сам
16:00+     Threads-ветка берёт ВЫШЕДШИЙ пост (его текст = то, что реально вышло, с правками)
           → мини-скоуп (1 тред + та же обложка) | мини-флагман (1-4 треда, текст)
           → ревью-копии в ОТЛОЖКУ тестового Threads-канала на время публикации −4ч
           → владельцу уведомление на мейн
T−0        публикатор Threads ПЕРЕЧИТЫВАЕТ ревью-копию и публикует ТО, ЧТО ТАМ ЛЕЖИТ СЕЙЧАС
```

## 2. Три правила, на которых стоит вся схема

1. **Источник для Threads — ВЫШЕДШИЙ пост, не черновик.** Владелец правит ТГ-пост в отложке; если
   деривация пойдёт с черновика, в Threads уедет текст, который он уже вычеркнул. Журнал вышедших
   постов (`published_journal`) — единственный мост между пайплайнами.
2. **Отложка ревью-канала — это ПУЛЬТ, а не витрина.** Оставил как есть → уйдёт как есть. Поправил →
   уйдёт ЕГО версия, а не файл из памяти завода. Удалил сообщение → в Threads не уйдёт ничего.
   Никаких отдельных кнопок «подтвердить»: владелец уже живёт в нативной отложке.
3. **Правка = сигнал обучения, а не просто правка.** Каждое расхождение «наш текст ↔ то, что осталось
   в отложке» меряется в процентах (как в ТГ, `core/edit_delta`), помечает пост как «правленый» и
   копит класс правки. Повторяющийся класс → урок в свод СВОЕГО формата. Цель — чтобы правок
   становилось меньше, а не чтобы они просто фиксировались.

## 3. Что для этого нужно построить

| Шаг | Что | Зачем именно так |
|---|---|---|
| A | **Очередь Threads** `data/threads_queue.jsonl`: текст, формат, id ревью-сообщения, время публикации, статус | у Threads API НЕТ отложенной публикации (проверено: параметра времени не существует, вызов публикует немедленно) — время держим мы |
| B | **id ревью-сообщения** при постановке в отложку (`telegram_publish` уже знает сообщение; нужно вернуть его id наружу) | правку ищем ДЕТЕРМИНИРОВАННО: у отредактированного сообщения id тот же, у удалённого id исчезает. Сверять по совпадению слов — гадание |
| C | **Повторная проверка перед публикацией**: перечитать сообщение по id → нет его = отмена; текст изменился = публикуем ЕГО и метим `edited` | это и есть требование «публикуется отредактированный пост админа, а не из файлов памяти» |
| D | **Замер правки** (`edit_delta` для Threads) + запись в журнал переработок | «сделай, чтобы редактирования было минимально» — без числа этого не измерить |
| E | **Публикатор из очереди**: линт охвата → квота Meta → ЗАПИСЬ открывается на ОДИН вызов с причиной и закрывается | правило «аккаунт важнее данных» остаётся живым и в автономии |
| F | **Третий выключатель автопилота** `data/autopilot_on_threads` + своё время выхода | форматы независимы: ТГ может идти, Threads стоять, и наоборот |

## 4. График (ответ владельца 09.09)

- **Threads выходит РОВНО тогда же, когда ТГ** — 16:00. Не вечером, не на следующий день.
- **Серия мини-флагмана:** первый тред в то же время, каждый следующий — через 2 часа (16:00, 18:00,
  20:00, 22:00). Разрыв в часы, о котором говорит площадка, соблюдён, серия укладывается в день.
- ⚠️ **Следствие, которое меняет правило №1.** Раз оба выходят в 16:00, переработать «уже вышедший»
  пост невозможно — нечего было бы отдавать на ревью за 4 часа. Значит источник для Threads на
  боевом пути — **текст ТГ-поста из ОТЛОЖКИ на момент T−4ч** (самая свежая версия с правками
  владельца на тот час), а не запись журнала, сделанная при постановке. Журнал остаётся входом для
  ручных прогонов и обкатки. Финальную правку всё равно ловит ревью-копия: она и есть последнее
  слово перед публикацией в Threads.
- **Право пропустить** (может ли мини-скоуп сказать «человеческого угла нет») — вопрос открыт.

## 5. Картинки

Мини-скоуп берёт обложку ТГ-поста (она уже отобрана и опубликована), мини-флагман идёт текстом —
решение владельца 09.09. ⚠️ Threads API принимает картинку **только публичной ссылкой**: локальный
файл ему не отдать. Значит к вехе E нужен URL первоисточника кадра — сейчас мы храним только путь к
скачанному файлу.

## 6. Аудит готовности к «одной кнопке» (11.09.2026)

**Цель владельца:** одно нажатие (или автопилот) → ТГ-пост формата дня в ТГ-отложке + Threads-версия(и)
в отложке ревью-канала + один итог. Публикация в сам Threads — пока руками, в кнопку не входит.

Три независимых проверяющих прошли мини-флагман, мини-скоуп и отказы цепочки (только чтение кода и данных).
Живой прогон мини-скоупа 11.09 прошёл успешно: взял оставшийся в канале пост, свою обложку, $0.04.

### 6.1 Уже исправлено 11.09 (коммиты 6ee3127 … этот)
Сверка журнала с каналом (удалённый из отложки пост в Threads не идёт) · номер сообщения в журнале +
подстраховка каналом/временем · копия обложки в `data/journal_covers` (кадры `scope_N_M.jpg` перезаписывает
каждый прогон) · откат на уже переработанный пост = стоп, повтор свежего = «⚠️ ПОВТОР» · без квитанции
постановки ТГ-пайплайн не пишет журнал и не метит тему · журнал переработок — только легшие посты ·
шапка ревью-копии убрана · слот своего формата + шаг серии 2 ч · эхо «❌ [N знаков]» срезается ·
неопознанный ревью-канал = стоп · уведомление не роняет прогон и сообщает о провале · argparse +
UTF-8 консоль · два теста больше не пишут в боевой `data/`.

### 6.2 Блокеры кнопки (НЕ исправлены — меняют устройство, ждут решения)

| # | Что | Где | Сценарий | Фикс |
|---|---|---|---|---|
| Б1 | Threads не знает, какой пост сделал ЭТОТ прогон | `run_threads_cycle` сам зовёт `threads_source.resolve` | ТГ-шаг не поставил пост (у скоупа «повода нет» — штатно) → Threads берёт прошлый пост формата; если он не перерабатывался — серия молча уйдёт по старому посту | `run_cycle` возвращает `{ok, kind, receipt, entry}`; `run_threads_cycle(src=entry, strict=True)`; нет квитанции — Threads-шаг не запускается и пишет причину; в строгом режиме повтор = стоп |
| Б2 | Замок только в `run_pipeline.main` | `run_autopilot.py:197`, `creator_bot` `/run`, `/run_scope`, `/run_threads`, `run_threads_pipeline` | Автопилот идёт, владелец жмёт запуск → оба берут «самый свежий драфт» → один пост дважды в отложке. `acquire` не атомарен | Замок внутри `run_cycle`/`run_threads_cycle`, файл замка через `O_EXCL`; держит оркестратор цепочки |
| Б3 | Запасной путь «ревью → PUBLISH_CHANNEL» | `run_threads_pipeline` (выбор канала) + поиск канала по подстроке названия `publish.py:98` | Битый `plan_settings.json` (уже было 26.08) → серия в основной канал на 16:00 рядом с настоящим постом | В режиме кнопки: нет ревью-канала или он = `PUBLISH_CHANNEL` → отказ; адрес только id/инвайт |
| Б4 | Повтор после сбоя = дубли | ТГ: `mark_run` до прогона; Threads: постановка без сверки ревью-отложки | Процесс убит посреди серии → повторный запуск ставит всё заново на те же минуты | Перед постановкой сверять `scheduled_texts` ревью-канала; каждый легший пост писать с номером |
| Б5 | «Сироты» при посте из двух сообщений | `publish.py` — повтор «чистым текстом» на ЛЮБОЕ исключение | Фото ушло, текст упал (сеть/FloodWait) → фото ещё раз документом → в 16:00 выходят картинки без текста, отчёт «ничего не ушло» | Повтор только на ошибку разметки/медиа; отправленное удалять или возвращать номера |
| Б6 | Проверки-пустышки | `_scheduled_async` → `[]` при сбое; «Проверка: N сообщ.» ни с чем не сравнивает; слот автопилота из вердикта, а не из квитанции | Сессия умерла → «день свободен» → два поста в день; поздний финиш → пост на другом дне, итог врёт | Сбой = `None`/исключение; N до и после (+1); слот из `receipt.scheduled_at`, не сегодня → алерт |
| Б7 | Нет единого итога, провалы молчат | 3–4 сообщения в двух чатах; Threads уведомляет через MTProto — самый вероятный сбой; `cost.reset()` в Threads-шаге | Ночью Threads упал — никто не узнал; цена цепочки неизвестна | В цепочке шаги не уведомляют; один итог через `bot_alert` (Bot API): ТГ ✅/⛔ + Threads ✅/⛔/❌ + сумма $; файловый лог обоих пайплайнов; `reconfigure` в `run_pipeline` |
| Б8 | Одна MTProto-сессия, ~15 подключений за цепочку | `collect._client`, инвайт-ссылка резолвится на каждый вызов | Бот и ручной запуск одновременно → sqlite «database is locked»; частые `CheckChatInvite` → FloodWait (предположения) | Один клиент на прогон; межпроцессный замок вокруг сессии; кэш id канала |

**Мелкое:** журналы дописываются без проверки перевода строки/`fsync` (обрыв склеит две записи) ·
`date.today()` — системный пояс, не пояс плана · пустая тема скоупа даёт ложный «ПОВТОР» · в журнале
переработок нет формата · замок протухает через час.

### 6.3 Порядок сборки (каждый шаг — отдельный коммит с тестом)
1. **Контракт результата + строгий Threads** (Б1). Тест: ТГ-постановка упала → Threads не вызван; успех → `entry.tg_msg_id` доехал.
2. **Замок на цепочку** (Б2). Тест: второй запуск при живом замке — отказ, а не второй пост.
3. **Защита каналов** (Б3). Тест: пустой ревью-канал / = боевому → отказ.
4. **Оркестратор `run_chain(kind)`** — зовут автопилот и новая команда бота; Threads только при `ok`; единый итог через Bot API (Б7). Тест с подменёнными шагами; сейчас `check_once/_run` автопилота не покрыты ничем.
5. **Идемпотентность и «сироты»** (Б4, Б5, Б6).
6. **Живая проверка на Windows:** цепочка с `--review-only` для Threads → затем автопилот скоупа.

**Цена и время полного прогона** (по `data/cost_log.jsonl` с 20.08, оценка): ТГ-флагман ~$1.6–2.1,
ТГ-скоуп ~$2.0–2.9, Threads +$0.04–0.33; обычно 7–12 мин + 1–5 мин. Неделя ≈ $11–13, месяц ≈ $45–55.

### 6.4 Решения владельца (11.09.2026)
1. **`PUBLISH_CHANNEL` — тестовый канал**, в боевой владелец переносит руками. Следствие: «пост в ленте»
   = «не удалён из теста». При ручном Threads-запуске с несколькими версиями одного дня выбор берёт самую
   свежую из оставшихся; в цепочке кнопки вопрос снимается — Threads получает запись своего прогона (Б1).
2. **Ревью-копия — как ТГ: слот формата 16:00, серия флагмана через 2 ч.** Так уже сделано (04de240).
3. **«Пост всегда должен быть: нет сценария, при котором Threads не делается, когда есть ТГ-пост».**
   Для Б1 это значит: Threads-шаг делается ВСЕГДА для ТГ-поста этого прогона. Отсутствие ТГ-поста —
   не штатный «пропуск», а сбой ТГ-шага: алерт с причиной, Threads по старому посту НЕ делается.
   ⚠️ Сверить с правилом автопилота 26.08 «скоуп без повода = штатный исход, алерт это различает» —
   по новому решению кнопка должна доводить ТГ-скоуп до поста (как ручной запуск, память 22.07).
4. **Итог полного прогона — в личку мейну (главному админу) как отчёт** (`PUBLISH_NOTIFY`, аккаунт-публикатор).
   Промежуточные уведомления шагов в цепочке выключаются. Провалы — дополнительно через бот-алерт (Bot API),
   который уже есть у автопилота: итог в личку не дойдёт именно тогда, когда умерла MTProto-сессия.

## 7. Парковка идей (НЕ строим сейчас)

- **Сторис канала + ссылка в комментах** (идея владельца 09.09, он делает это руками): после выхода
  поста — репост в сторис канала, в комментарии под постом ссылка на пост. **Проверено 09.09:
  выполнимо нашим же стеком, бусты у канала есть.**
  - Сторис: `stories.CanSendStoryRequest(peer)` — предполётная проверка, что каналу сейчас можно;
    `stories.SendStoryRequest(peer=канал, media=обложка поста, privacy_rules=[...], caption=...,
    media_areas=[InputMediaAreaChannelPost(координаты, канал, msg_id)], period=86400)`. Именно
    `MediaAreaChannelPost` делает сторис НАТИВНЫМ репостом: тап по области открывает сам пост.
  - Комментарий: `messages.GetDiscussionMessageRequest` находит копию поста в связанной группе
    обсуждения, ответом туда уходит ссылка. Всё есть в Telethon 1.44, который у нас стоит.
  - Осталось решить деталями: какую картинку класть в сторис (обложку поста), где ставить область
    ссылки, и запускать сразу после публикации (сторис на неопубликованный пост не сошлётся).

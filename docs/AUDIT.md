# Аудит проекта TG-Agents

> **Живой документ.** Здесь оценивается профессиональная зрелость проекта и ведётся
> панч-лист фиксов. Обновляется в любой сессии: переаудит → правка баллов → отметка статусов →
> запись в changelog. Цель — чтобы в любой момент было видно «насколько мы профессиональны» и
> «что чинить дальше», без повторного разбора с нуля.

**Последний аудит:** 2026-07-17 (ПОЛНЫЙ, 8 осей) · **Общая оценка: 7.2 / 10** — флэт к 7.3, здоров.
**Статус на 17.07.2026:** ни одного нового бага в критпути — все находки P2 + два отложенных P1
(отказоустойчивость). Замкнут ТГ-контур само-обучения (пикер флагмана учится на ТГ-сигнале). Две
сквозные темы, обе — не баги: (1) **doc-drift** (рецидив N-24) — петля подключена 17.07, но
ARCHITECTURE/README/self_learn_check описывали её как «дормант» и с обратными весами → карта врала
non-coder-владельцу (закрыто в тот же день, N-37); (2) **построено-впрок** — Threads-половина
self-learn (~26КБ, 12 модулей) гоняется на НУЛЕ датапоинтов, бандит-плюмбинг под ~2 флагмана/нед =
над-инженерия под solo (заморожено до данных, N-38/N-47). Мой свежий §4.2-код повторно завёл на живом
пути пикера три закрытых класса (N-10/N-11/N-15 → N-41/N-42/N-43) — починены в тот же день.
Осознанные отсрочки держатся: SPOF MTProto/хартбит — триггер «автопилот/публичный релиз».

---

## Как обновлять этот файл

1. **Переаудит** (раз в N недель или после крупных изменений): прогнать 4 оси параллельными
   read-only агентами — архитектура, безопасность, качество кода, инженерная зрелость. Промпты-скелеты
   и охват — см. «Методология» ниже. Каждая ось → балл 1–10 + находки с `file:line`.
2. **Обновить баллы** в таблице «Скоринг» (добавить новую строку с датой, старые НЕ стирать — видно динамику).
3. **Статусы фиксов:** `☐` открыто · `◐` в работе · `☑` сделано (дата + хеш коммита) · `🗑` отменено (почему).
   Новые находки добавлять с новым ID (не переиспользовать освободившиеся).
4. **Changelog** внизу — одна строка на изменение аудита.
5. **Не регрессировать сильные стороны** (раздел ниже) — это то, что уже сделано правильно.

## Методология (чтобы переаудит был воспроизводим)
4 независимые оси, каждую ведёт отдельный read-only агент (general-purpose), синтез — у ведущего:
- **Архитектура:** слои (движок/руки/агенты/память), «мозги vs руки», DRY, coupling, расширяемость,
  согласованность ARCHITECTURE.md с кодом. Читать: `core/{agent_runtime,llm,config,memory,analytics}.py`,
  все `*_bot.py`/`*_tools.py`, `workshop.py`, `agents/*/`.
- **Безопасность:** секреты (.env/config), `.gitignore`, коннекторы (twikit-куки, telethon-сессии),
  subprocess/eval, авторизация ботов, гейт девелопера. grep на хардкод-ключи.
- **Качество кода:** обработка ошибок (Claude API, сеть, парсинг), async-корректность (блокировки loop),
  дублирование, робастность JSON/CSV/MD, логирование, type hints.
- **Инженерная зрелость:** тесты, CI, lock-файл, Docker/процесс-менеджмент, бэкапы, документация, онбординг.

### 5-я ось — ENFORCEMENT (per-function, адверсариально) — добавлена 20.07
Урок 20.07: 4 полных 8-осевых аудита (через того же ассистента) НЕ нашли проблем, которые за день
вскрыл узкий проход. Причина — ВЫСОТА, не инструмент: 8 осей ловят СТРУКТУРУ (секреты/тесты-есть/
doc-drift/CI), но НЕ ПОВЕДЕНИЕ. Отдельная ось: проход по КАЖДОЙ важной функции — проследить поток
данных и сравнить НАМЕРЕНИЕ с ПОВЕДЕНИЕМ («гейт реально стопит? писатель бережёт корпус? регекс ловит,
что заявляет?»). Рамка АДВЕРСАРИАЛЬНАЯ (задача агенту — СЛОМАТЬ, дать строки-что-проскакивают), не
оценочная. Приоритет: без тестов × центральный × недавно менялся × цена ошибки. Регулярно, не разово.
Так найдены N-50…N-61, часть — в «проверенном» коде (TG-сборщик жил дырой N-60 через 4 аудита: слепая
зона «хорошо по аналогии»). **Правило-спутник:** починил паттерн на одном близнеце (Threads `_merge_keep`)
→ сразу проверь второго (`telegram_export/collect`) — фикс на одном не переезжает на старого сам.

**Как проследить историю функции с причинами (для будущего) — ДВА индекса, оба ведут к «почему»:**
1. **По коду (git):** `git log --follow -- core/<файл>.py` — вся цепочка правок файла; `git log --grep="<тема>"`
   — по теме (Скаут/2FA/уроки). Тело каждого коммита несёт ЧТО/ЗАЧЕМ. Конвенция это обеспечивает:
   scope-тег в заголовке (`fix(scout):`, `feat(lessons):`) + причинно-следственное тело + ссылка на N-XX.
2. **По аудиту (этот файл):** пункт N-XX = находка + причина + статус + ХЕШ коммита. От находки прыгаешь в
   код, от кода — в findings. Правило: каждый N-XX-фикс несёт свой хеш, каждый фикс-коммит — свой scope-тег.

---

## Скоринг (динамика)

| Дата | Архитектура | Безопасность | Качество кода | Инж. зрелость | Общая |
|---|---|---|---|---|---|
| 2026-06-17 | 8 | 6.5 | 7 | 5 | **6.5** |
| 2026-06-23 (P0-фиксы) | 8 | 8 | 7.5 | 5.5 | **7** | <!-- закрыта P0-зона: OWNER_ID, try/except, баг токена -->
| 2026-06-23 (переаудит) | 8 | 7 | 7.5 | 5.5 | **7** | <!-- новый P0-16: захардкоженный api_hash в истории git → безопасность 8→7 до фикса -->
| 2026-06-23 (итог дня) | 8 | 8.5 | 8 | 6.5 | **7.5** | <!-- P0-16 закрыт (секрет вычищен+force-push), SSRF, тесты(32), lock+.python-version, ARCHITECTURE.md -->
| 2026-07-09 (переаудит) | 8 | 8 | 7.5 | 6 | **7.3** | <!-- рост кода (scope/Threads/tg_scoring) обогнал тесты/ops-долг; калиброванный минус без инфляции -->
| 2026-07-09 (полный, 8 осей) | 8 | 8 | 7.5 | 6 | **7.1** | <!-- +Мусор 7.0 +Отказоустойчивость 6.0 +Данные 7.2 +Продукт-фит 6.5; overall взвешен по 8 осям -->
| 2026-07-15 (полный, 8 осей) | 8 | 8 | **8** | **6.5** | **7.4** | <!-- +Мусор 7.0 +Отказоустойчивость 6.5 +Данные 7.5 +Продукт-фит 7.5; качество/зрелость выросли делом (io_safe, fail-open/closed, CI 135 тестов, _guard); продукт-фит: Threads из задела стал фичей -->
| 2026-07-17 (полный, 8 осей) | 8 | 8 | 7.5 | 6.5 | **7.2** | <!-- +Мусор 7.5 +Отказоуст 6.5 +Данные 7.5 +Продукт-фит 7; замкнут ТГ-контур обучения; качество 7.5 (не 8): §4.2-код завёл N-10/11/15 на живом пикере; главные темы — doc-drift (N-37) + построено-впрок (N-38); почти всё P2 починено в тот же день -->
| 2026-07-20 (инкремент, сессия гейтов/уроков/линтера/Скаута) | 8 | 8 | **7.7** | **6.9** | **7.4** | <!-- +Отказоуст 7 (детект тихой деградации Скаута); качество: фикс антитезы-линтера (портил подачу) + cost-мис-атрибуция funnel восстановлена; зрелость: ~30 новых тестов (linter-fuzzy/usefulness/lessons/scout); +анти-жир уроков (lifecycle, -24% контекста) + гейты scope (свежесть+польза); всё аддитивно, happy-path не тронут -->


---

## Сильные стороны (НЕ регрессировать)

- **«Мозги vs руки» реально соблюдён.** `agent_runtime.run()` параметризован (`tools_schema`/`dispatch`/
  `system_builder`) — движок не знает агентов, агенты импортируют движок. Инверсия зависимостей.
- **DRY-рантайм:** один движок на 5 агентов, `*_bot.py` — тонкая декларативная обвязка (40–175 строк).
- **Структурные гейты кодом, не промптом:** `workshop.apply()` промоутит только существующий `.proposed`
  (+ бэкап в `.history/` + защита от path-traversal); `creator._apply_standard` — так же.
- **Грабли Anthropic API учтены:** починка обрыва `tool_use/tool_result` (`_trim_history`), пустой
  `tool_result`→заглушка, `pause_turn`, `busy`-замок от дублей, `to_thread` для синхронного SDK.
- **Гигиена секретов:** `.gitignore` исчерпывающий (`.env*`, `*.session`, `data/`, owner-файлы); ноль
  хардкод-ключей; `yaml.safe_load`; subprocess без `shell=True`/ввода пользователя.
- **Детерминированные линтеры** (`creator_tools._lint`): типографика/размер/стаккато/жирный кодом, счёт
  длины в UTF-16 под лимит Telegram.
- **Документация объясняет решения** (ARCHITECTURE.md: почему код агентов плоско в `core/`).

---

## Панч-лист

### 🔴 P0 — до первого живого/публичного прогона (дёшево, в общем коде)

- ☑ **P0-1 [security] Авторизация по OWNER_ID.** ✅ СДЕЛАНО 23.06.2026 — `config.owner_ids()` (`OWNER_ID`
  в `.env`, список через запятую) + один `outer_middleware` на `dp.message` в `agent_runtime` гейтит ВСЕ
  хэндлеры до фильтров; не-владелец молча отсекается. Пусто = гейт выкл + громкий warning на старте.
  Добавлена `/whoami` (узнать свой id). Закрывает все 5 ботов разом. ↓ исходный контекст:
- ~~☐ **P0-1 [security] Авторизация по OWNER_ID.**~~ Сейчас `@dp.message()` (`agent_runtime.py:226`) шлёт
  сообщение ЛЮБОГО отправителя в модель с полным набором инструментов — нет сверки `m.from_user.id` с
  allowlist. Любой по @хэндлу: жжёт Claude-бюджет, вытаскивает профиль/задачи из контекста, через дев-бота
  правит SKILL'ы (гейт проверяет процедуру, не «кто»); плюс `_write_owner` отдаёт адрес проактивных
  отчётов «последнему написавшему». **Фикс:** `OWNER_TELEGRAM_ID` (список) в `.env`, гейт в начале
  `_turn`/`_start`/пресет-хендлеров → не-владельцу плоский отказ, `_write_owner` только для владельца.
  Один гейт в общем рантайме закрывает все 5 ботов.
- ☑ **P0-2 [quality] try/except вокруг Claude-вызова и dispatch.** ✅ СДЕЛАНО 23.06.2026 — per-tool
  try/except вокруг `dispatch(...)` в `llm.py` (кривой инструмент → ошибка в tool_result, не падение хода)
  + try/except вокруг вызова LLM в `agent_runtime._turn` (сбой create/доставки → владельцу «сбой, см. лог»,
  стек в `logging.exception`, история хода НЕ портится). ↓ исходный контекст:
- ~~☐ **P0-2 [quality] try/except вокруг Claude-вызова и dispatch.**~~ `llm.py:62` (`client.messages.create`)
  и `llm.py:84` (`dispatch(...)`) — голые, в `while True`. При исчерпании ретраев SDK / 401 / битом JSON
  в `data/` исключение всплывает через `to_thread`, ответ юзеру НЕ уходит → «бот молча завис». Только
  `dev_tools.dispatch` обёрнут. **Фикс:** обернуть оба места в `llm.py`, вернуть человекочитаемый текст /
  `tool_result(is_error)`; логировать `resp._request_id`. Одна точка защищает всех агентов.
- ☑ **P0-3 [arch] Баг имени токена в main.py.** ✅ СДЕЛАНО 23.06.2026 — docstring `main.py:5` исправлен на
  `SECRETARY_BOT_TOKEN`. ↓ исходный контекст:
- ~~☐ **P0-3 [arch] Баг имени токена в main.py.**~~ `main.py:5` (docstring) говорит `TELEGRAM_BOT_TOKEN`, а
  `config.yaml`/`.env.example` ждут `SECRETARY_BOT_TOKEN`. Новый пользователь впишет не тот ключ →
  RuntimeError. **Фикс:** заменить на `SECRETARY_BOT_TOKEN`. 1 строка.

- ☑ **P0-16 [security] Захардкоженный Telegram API_HASH + телефон в коде, в истории git.** ✅ СДЕЛАНО
  23.06.2026 — (1) секрет убран из кода (`make_session.py` читает из `.env`); (2) история git очищена
  (`filter-branch` по `make_session.py` + `docs/AUDIT.md`, продут gc) — номер/хэш/api_id отовсюду удалены
  (0/0/0); (3) **force-push на GitHub** (`b246f31→341d1bc`), origin синхронен. NB: ротацию самого api_hash
  НЕ делали — Telegram не даёт пересоздать hash приложения; репо приватный, hash≠токен/сессия (низкий риск).
  Остаточно: GitHub может держать «висячие» старые коммиты по прямому SHA до своего GC (приватный, без
  форков — практически недоступны). ↓ исходный контекст:
- ~~☐ **P0-16 [security] Захардкоженный Telegram API_HASH + телефон.**~~ НАЙДЕНО при
  переаудите 23.06.2026. `connectors/telegram_export/make_session.py:21-22` содержал реальные `API_ID=REDACTED`
  и `API_HASH="cb9e44…"`, в докстроке `:4` — реальный телефон (PII, redacted). Файл трекается; api_hash
  **подтверждённо в истории** (коммит `a0fe09b`). Серьёзность УМЕРЕННАЯ: это app-hash + PII, не бот-токен и
  не сессия (аккаунт им не угоняют), репо приватный. `.env`/`*.session`/куки в индексе чисты. **Фикс:**
  (1) вынести в `.env` (`TELEGRAM_API_ID/HASH` уже есть в config) + убрать номер из докстроки; (2) ротировать
  api_hash на my.telegram.org; (3) решить — чистить ли историю (`git filter-repo`). **РИСК ПРАВКИ: низкий** —
  `make_session.py` разовый помощник, в рабочем конвейере не участвует.

### 🟡 P1 — воспроизводимость и надёжность (дёшево, большой эффект)

- ☑ **P1-4 [ops] Lock зависимостей + потолок anthropic.** ✅ СДЕЛАНО 23.06.2026 — `anthropic>=0.40,<1` в
  requirements.txt + `requirements.lock` (точные пины из рабочего venv, Python 3.13).
- ◐ **P1-5 [ops] Базовые тесты на чистые функции.** ✅ В основном СДЕЛАНО — `tests/` есть (9 файлов:
  dedup, content_plan, creator_lint, text_helpers, tg_format, market, workshop, ssrf, analytics_registry);
  09.07 добавлены `_hits_paused` (граница слова) и `llm.resolve_thinking`. Остаток: нет CI-прогона
  (P2-12) и покрытия свежих `verify`/`scope_writer`-хелперов; запуск ручной `python -m pytest`.
- 🗑 **P1-6 [ops] Рабочий запуск в Docker + рестарты.** — ОТЛОЖЕНО РЕШЕНИЕМ ВЛАДЕЛЬЦА 12.07: деплой далеко,
  автопилот/хартбит делаем ПОСЛЕ доводки Криейтора (хартбит — самым последним). Пока владелец сам в цикле
  как редактор — ручной `python run_*.py` это фича (видит каждый пост), а не долг. Вернуться, когда захочет
  «крутится само». Задел: `Dockerfile.bots` (python:3.13 + `pip install -r`) + compose `restart: unless-stopped`
  на агента, либо systemd/supervisor. Текущий `Dockerfile` поднимает Claude Code (node), НЕ Python-ботов.
- ◐ **P1-7 [quality] Защита парсинга JSON/MD.** ЧАСТИЧНО: `core/io_safe.load_json` внедрён в JSON-путях
  (N-10 + N-10-доп 12.07). `memory._load_tasks` — снят с учёта (`memory.py` удалён с ассистентом). Остаётся
  ревизия `read_text`/`.md`-парсинга в `creator_tools`/`scout_tools`/`analytics._load_posts` на предмет
  «битый файл роняет инструмент». **Фикс:** прогнать оставшиеся `read_text` через тот же паттерн default+лог.

### 🟢 P2 — чистота кода и техдолг

- ◐ **P2-8 [arch/quality] Общий реестр аналитических tools.** ✅ ЧАСТИЧНО 23.06.2026 — общий **dispatch**
  вынесен в `core/analytics_tools.py` (см. P2-13). **СХЕМЫ намеренно НЕ объединены:** их описания у агентов
  разные не случайно, а под роль (Скаут `find_posts`=«дедуп», Криейтор=«анти-повтор», Аналитик=«сравнение»)
  — это не дубль, а полезная подсказка модели. `top_posts` тоже оставлен в агентах (дефолт metric у Аналитика
  `views`, у Криейтора `er`). Вывод: «дубль схем» оказался ролевой кастомизацией — трогать НЕ надо.
- 🗑 **P2-9 [arch] Персистентность истории диалога.** — БОЛЬШЕЙ ЧАСТЬЮ МООТ 12.07: главный потребитель
  (личный ассистент/живой ТуДу) удалён; оставшиеся боты команд-driven, `run_pipeline` one-shot — терять
  нечего. `history` в RAM (`agent_runtime`) остаётся, но приоритет снят. Вернуть, если появится диалоговый агент.
- ☑ **P2-10 [docs] Починить рассинхрон ARCHITECTURE.md с кодом.** ✅ СДЕЛАНО 23.06.2026 — ARCHITECTURE.md
  переписан под реальный код (Криейтор с ~17 инстр.; добавлены конвейер, карта файлов-шины, авторизация,
  точки хрупкости, как добавить агента/коннектор). Онбординг-документ.
- ☑ **P2-11 [security] SSRF-фильтр в fetch_page.** ✅ СДЕЛАНО 23.06.2026 — `_url_blocked_reason` резолвит
  хост и блокирует private/loopback/link-local/reserved (вкл. `169.254.169.254`, `127.0.0.1`); кастомный
  `_SafeRedirect` перепроверяет каждый редирект; покрыто `tests/test_ssrf.py` (5 кейсов).
- ◐ **P2-12 [ops] CI + линтеры + версия Python.** ✅ ЧАСТИЧНО 12.07: GitHub Actions `.github/workflows/tests.yml`
  — авто-прогон `python -m pytest` (Python 3.13, `requirements.lock`) при каждом push/PR (красный крест = поломка,
  не запуская руками). `.python-version`=3.13 уже был (N-1). ОСТАЛОСЬ (осознанно отложено): `pyproject.toml` +
  ruff/mypy — линтер на неотревиженном коде даст шум и красный CI без пользы для non-coder-владельца; вводить
  постепенно, когда/если будет ценность.
- ☑ **P2-13 [quality] dispatch как таблица, не if-цепочка.** ✅ СДЕЛАНО 23.06.2026 — общие read-only
  аналитич. инструменты (`channel_summary`/`find_posts`/`by_theme`/`themes_overview`/`by_dimension`/
  `recent_posts`) вынесены в `core/analytics_tools._SHARED` (dict имя→вызов); Скаут/Аналитик/Криейтор зовут
  `analytics_tools.handle(name,args)` в начале своих dispatch, дубли-ветки удалены. Поведение идентично
  (те же вызовы/дефолты), покрыто `tests/test_analytics_registry.py`. Агент-специфичные tools остались
  в своих файлах.
- ☑ **P2-14 [ops] Бэкап памяти/данных.** — ✅ СДЕЛАНО 12.07: `backup.py` одной командой архивирует ВСЁ
  незаменимое (`.env`, `data/evgeniyp.session`, `threads_token`, `gpt_profile`, owner-файлы, `cost_log`, вся
  `memory/`) кросс-платформенно; скретч намеренно пропускает. Рунбук — `docs/OPERATIONS.md` (было закрыто N-5,
  теперь автоматизировано). Тест `tests/test_backup.py`. Версионирование схемы JSON — над-инженерия, не сейчас.
- ☑ **P2-15 [quality] Наблюдаемость.** — ✅ СДЕЛАНО 12.07: `logging_setup` инъектит `[agent/req]` в КАЖДУЮ
  строку через contextvars + хендлер-фильтр (формат `... name [agent/req]: msg`). `set_agent` — на старте бота
  и пайплайна; `new_request(uid)` — на каждый ход `_turn`/долгую команду и на запуск пайплайна. Тест
  `tests/test_logging_setup.py`. Остаток (не срочно): протянуть `req` в этапы `run_pipeline`, `resp._request_id` в лог при API-сбоях.

---

## Переаудит 2026-06-23 — новые находки и пересмотр приоритетов

Прогон 4 read-only ревьюеров после закрытия P0-зоны. Подтверждены и валидны все открытые P1-4…P2-15.
Ниже — только НОВОЕ и изменения важности.

### Новые находки
- ☑ **N-1 [ops/P1] Рассинхрон версии Python.** ✅ СДЕЛАНО 23.06.2026 — `.python-version`=3.13 под реальный
  venv. (`requires-python` в pyproject — когда заведём pyproject, P2-12.)
- ☑ **N-2 [ops/P2] Дублирующийся `logging.basicConfig`** — ✅ СДЕЛАНО 12.07 (`42ab4a7`, см. changelog: единый `core/logging_setup`). Ниже — исходный контекст. `agent_runtime.py:28`, `run_pipeline.py:23`,
  `publish.py` — повторные вызовы no-op, формат не настроен (нет времени/уровня/имени). Единый
  `core/logging_setup.py`. Смыкается с P2-15. Риск: низкий.
- ☑ **N-3 [ops/P2] `data/` мешает живое состояние и скретч.** — ✅ СДЕЛАНО 12.07: классификация «живое vs
  скретч» зафиксирована В КОДЕ (`backup.py` списки `IRREPLACEABLE`/`SCRATCH`, `--list`) — единый источник
  правды, вместо только-текста. Физически пути НЕ двигали (опасно для `*.session`). `analyze.mjs`/`result.json`
  (legacy-скретч, не в git) — помечены в `SCRATCH`, владелец удаляет у себя локально. Риск: нулевой.
- ☑ **N-4 [ops/P2] Windows `.bat` закоммичены** — ✅ ЗАКРЫТ (вычищены ранее; на диске/в git отсутствуют, проверено 12.07). Исходный контекст: (`install.bat`, `start_analyst.bat` — только для 1 из 5
  агентов, несимметрично). Удалить или вынести в `scripts/win/` симметрично. Риск: низкий.
- ☑ **N-5 [security/ops/P2] Бэкап `data/*.session` не задокументирован** — ✅ СДЕЛАНО 09.07.2026 —
  `docs/OPERATIONS.md`: рунбук бэкапа невосстановимого (session/.env/threads-токен/gpt-профиль/живая memory,
  PowerShell-архив) + silent-failure чек «жив ли завод». Само копирование делает владелец на своей машине.
- ☑ **N-6 [security/P2] Prompt-injection из внешнего контента.** — ✅ СДЕЛАНО 12.07 (двухслойно):
  (1) ЧТЕНИЕ — весь внешний текст (RSS/ТГ-каналы/твиты/произвольная страница/бриф) идёт в модель через
  `untrusted.wrap` («это ДАННЫЕ, не команды») — было построено ранее, проверено что подключено на всех входах.
  (2) ЗАПИСЬ — новый детерминированный страж `untrusted.reject_if_framed` в ДОЛГОЖИВУЩИХ инструментах
  (`record_lesson`, `propose_standard`, `save_playbook`, `x_scan.update_author`): нельзя копировать обёрнутый
  внешний контент в постоянную память (ловит «отмывание» инъекции). `apply_standard` уже за гейтом владельца.
  Тест `tests/test_untrusted.py`. Остаток (accepted): семантический перефраз инъекции детектор не ловит —
  для solo с ревью владельца приемлемо (LLM-судья на каждую запись = над-инженерия).
- ☑ **N-7 [docs/P2] Рассинхрон docstring `publish.py`** — ✅ СДЕЛАНО 23.06.2026 — модульный docstring
  приведён к коду (файлом без хостинга; рендер HTML через tg_format), убраны «3 пути»/parse_mode=None/17.png.
- ☑ **N-8 [security/P1] `_write_owner` до проверки гейта.** — ✅ СДЕЛАНО 12.07 (`42ab4a7`: `_record_owner_chat` пишет только при `uid in allowed`). Исходный контекст: При пустом OWNER_ID первый написавший становится
  адресатом проактивных отчётов. Брать адресата из `OWNER_ID`, писать owner.txt только для allowed. Риск: низкий.

### Пересмотр важности существующих
- **P2-8 + P2-13 → поднять до P1.** Дубли tool-схем/dispatch в 3-5 файлах — единственная архитектурная вещь,
  что «масштабируется копипастой»: сейчас 3 копии, при 8 агентах будет 8, уже с РАСХОЖДЕНИЯМИ описаний
  (риск качества, не только код). Чинить ИМЕННО сейчас, пока база маленькая. **РИСК ПРАВКИ: средний** —
  описания load-bearing для модели; делать со сверкой схем 1:1 + прогон в /test.
- **P2-9 → поднять до P1.** История в RAM прямо бьёт по заявленному главному агенту (личный ассистент/живой
  ТуДу теряет контекст на рестарте). Персист на диск по образцу owner-файлов. Риск: низкий.

### Сильные стороны, подтверждённые переаудитом (не регрессировать)
- Ядро спроектировано под рост корректно: инверсия зависимостей соблюдена (агенты → ядро), `run()`
  полностью параметризован. Это главный актив для масштабирования.
- Десериализация безопасна (везде `yaml.safe_load`, без `pickle`/`eval`/`exec`); инструменты-записи
  защищены от path-traversal (`workshop._agent_dir`, санитизация имён в `_save_draft`/`_save_brief`).
- Креды внешних «рук» — только бёрнеры, в `data/` под gitignore.

### Рекомендованный порядок (под «навести порядок до роста, не сломать работающее»)
**Сейчас — аддитивно, низкий риск:**
1. **P0-16** — секрет make_session.py → `.env` + ротация + (решить про историю). Конвейер не трогает.
2. **N-8** — owner.txt только для allowed (1 условие).
3. **P1-4 + N-1** — `requirements.lock` + потолок anthropic + `.python-version` (3.13). Защита всего парка.
4. **P2-9 (P1)** — персист истории диалога на диск.
5. **P1-5** — 15-20 pytest на чистые функции (`tg_format`, `content_plan`, `_trim_history`, analytics-парсеры).
6. **P2-12 + N-2** — `pyproject.toml` (ruff/mypy) + CI + единый logging_setup.
7. **P2-11** — SSRF-фильтр в feeds.py. **N-3/N-4/N-7** — гигиена (удалить мёртвое, поправить docstring).

**Сейчас — но средний риск (со сверкой и прогоном в /test):**
8. **P2-8 + P2-13 (P1)** — общий `core/analytics_tools.py` (реестр SCHEMAS+DISPATCH). Главный анти-копипаст.

**Потом / по необходимости:**
- **P1-6** — Docker под ботов + рестарты (средний риск: меняет модель запуска работающих ботов).
- **P2-14 + N-5** — бэкапы памяти и сессии. **P2-15** (остаток) — request_id в логах.
- **Threads-абстракция `Publisher`** — НЕ делать впрок (высокий риск, сердце конвейера); только когда
  реально появится вторая платформа.

### Карта связей (для онбординга — пойдёт в ARCHITECTURE.md)
Зафиксировано: граф запуска (точки входа→агент→токен→импорты), поток данных Скаут→Криейтор→Публикатор
через ФАЙЛЫ-шину (брифы/драфты/обложка), таблица «файл памяти → писатели → читатели», коннекторы и их креды,
10 точек скрытой связанности (один MTProto-клиент на 3 коннектора; файлы-шина без схемы «latest по mtime»;
content_plan — ручное зеркало стандарта; маркеры-протоколы `[[SPLIT]]`/`СТАТУС: ЧИСТО`; имя папки агента =
имя везде; глобальный `run_mode.txt`; байт-стабильность system для кэша; и др.). Детали → Фаза B.

## Переаудит 2026-07-09 — свежий прогон 4 осей (после scope/Threads/скоринга)

Workflow `reaudit-tg-agents` (4 read-only ревизора + синтез). Общая **7.5 → 7.3**: проект содержательно вырос,
но операционный/тестовый долг рос быстрее закрытий (калиброванный минус). Баллы: Архитектура **8** (держится,
изоляция scope↔флагман ПОДТВЕРЖДЕНА в коде), Безопасность **8.5→8**, Качество **8→7.5**, Инж-зрелость **6.5→6**.

### Новые находки
- ◐ **N-9 [ops/P1] 13 новых модулей без единого теста** — 3 core (`scope_writer`, `tg_scoring`, `verify`) +
  10 `connectors/threads/*`. Критично: `tg_scoring` УЖЕ в проде Аналитика (`analyst_tools.py:214` honest_ranking).
  ✅ 09.07: `tests/test_tg_scoring.py` — `_ranker`/`enrich`/зрелость/view_floor/тиры (синтетический корпус, без API).
  Остаток: `scope_writer._parse_media_subject`, `verify`, `threads` — следом (threads когда пойдёт в прод). Риск: средний.
- ☑ **N-10 [quality/P1] Голый `json.loads(read_text())`** (частичный регресс P1-7) — ✅ СДЕЛАНО 09.07 —
  `core/io_safe.load_json(path, default)` (нет/пусто/битый → default, INFO-лог, никогда не бросает; паттерн из
  `threads/auth.py`). Заменены 6 вызовов в `analytics.py` (topics/formats/posts/stats×2) и `memory.py` (tasks).
  Поведение на существующем валидном файле идентично; битый/пустой больше не роняет инструмент.
- ☑ **N-11 [quality/P2] `scope_writer`: широкие `except Exception`** — ✅ СДЕЛАНО 12.07 (`_vision_pick`/`_vision_ok`):
  разнесены `APIError` (транзиент, warning без трейсбека) / `(OSError, ValueError)` (данные, warning) / прочее
  (`logging.exception` — вероятный баг, но публикацию не роняем). Fetch og:image (:311) и 2FA (:353) — оставлены broad-но-логируемыми (внешний IO/фактчек, fallback безопасен).
- ☑ **N-12 [quality/P2] `_vision_pick`: base64 без кэпа размера** — ✅ СДЕЛАНО 09.07 (`fca648b`: кэп 4МБ). Исходный контекст: (`scope_writer.py:239`) — многомег-og:image → 400
  от Anthropic → тихий отвал ОБЯЗАТЕЛЬНОЙ обложки scope. **Фикс:** `st_size > 4МБ` → пропуск кандидата + warning. Риск: средний.
- ☑ **N-13 [security/P2] `config.owner_ids()` молча отбрасывает невалидные части** — ✅ СДЕЛАНО 09.07 —
  WARNING на отброшенные нечисловые части + ОТДЕЛЬНЫЙ warning на опасный случай «OWNER_ID задан, но все
  части мусор → гейт молча открыт всем». Предупреждает один раз за процесс (`_owner_warned` — owner_ids
  зовётся на каждое сообщение, не спамим).
- ☑ **N-14 [arch/P2] Инвариант изоляции scope↔флагман — только на дисциплине** — ✅ СДЕЛАНО 09.07 —
  `tests/test_isolation_scope_flagship.py`: статический разбор фактических `_read('memory/…')` в `scope_writer`
  и `creator_bot`, ассерт что scope не грузит FLAGSHIP_ONLY-файлы и наоборот (+ анти-vacuous страж). Точно по
  реальным загрузкам, не по упоминаниям в комментариях. Теперь протечка ловится тестом, а не только дисциплиной.
- ☑ **N-15 [quality/P2] `dedup._hits_paused` перекомпилирует regex каждый вызов** — ✅ СДЕЛАНО 12.07 (`42ab4a7`: `_paused_re` в `lru_cache`). Исходный контекст: (`:178`) — микро-неоптимальность
  (сам фикс границы слова КОРРЕКТЕН и покрыт тестами). **Фикс (опц.):** кэш `re.compile` для sticky — можно выкинуть.

### Регрессии (с ростом scope/Threads/скоринга)
- Покрытие тестами просело: +13 модулей без тестов, `tg_scoring` при этом уже в проде Аналитика.
- Качество нового кода ниже среднего по репо (scope_writer: широкие except, base64 без кэпа) — старый код (dedup/llm) аккуратнее.
- Два бага с 06-23 (N-10 голые json.loads; N-8 `_write_owner` до гейта) так и не закрыты.
- Threads расширил ops-поверхность (токен, окно refresh 60 дн) без авто-бэкапа/напоминания.

### Рекомендованный порядок (под solo-профиль: дёшево + низкий риск + реальная боль вперёд)
1. **N-10** `io_safe.load_json` — 6 голых json.loads (владелец правит `data/` руками → тихий креш). Риск: минимальный.
2. **N-9 (старт)** `test_tg_scoring.py` на чистые функции — код УЖЕ в проде. Риск: нулевой.
3. **N-13** `owner_ids()` лог + warning (1 строка). Риск: нулевой.
4. **N-12** кэп размера картинки перед base64. Риск: низкий.
5. **N-14** `test_isolation_scope_flagship.py` — страж красной линии 09.07. Риск: нулевой.
6. **N-11** разнести `except` на API/IO. Риск: низкий.
7. Тесты `verify`/`threads` — когда Threads пойдёт в прод (Фаза 2), не раньше.

**Осознанно в КОНЕЦ (над-инженерия под профиль):** CI/pyproject/ruff/mypy, structlog request_id, Docker
HEALTHCHECK+supervisor, SQLite-персист истории, авто-бэкап-cron, кэш regex, генератор `.bat` — перед публичным
релизом/наймом, не сейчас (бюджет-дисциплина + augmentation, не enterprise-хардненинг).

### Сильные стороны, подтверждённые (не регрессировать)
Изоляция scope↔флагман РЕАЛЬНА в коде (grep импортов памяти: `scope_writer` не тянет `content_manual/post_lessons`,
`creator_bot` не тянет `voice_core/scope_lessons`); мозги-vs-руки + инверсия зависимостей держатся; DRY на руках
(`creator_tools` в scope через `_DROP`); гигиена секретов цела; деградирующие отказы (vision/verify/dedup → текст/
дефолт, конвейер устойчив); `tg_scoring` честно спроектирован (MATURITY_DAYS/rate/view_floor/перцентили) —
недостаёт только тестов; грабли Anthropic API учтены (кэш, `_trim_history`, busy-замок, `untrusted.wrap`).

## Полный аудит 2026-07-09 (8 осей) — расширенный (мусор/отказоустойчивость/данные/продукт-фит)

Workflow `full-audit` (`.claude/workflows/full-audit.js`, 8 read-only ревизоров + синтез). 4 базовые оси держат
уровень 09.07 без регрессий (8/8/7.5/6 — подтверждено кодом: инверсия зависимостей, SSRF, OWNER-гейт, io_safe).
Новые 4 оси впервые: **Мусор 7.0**, **Отказоустойчивость 6.0 (слабейшая)**, **Данные 7.2**, **Продукт-фит 6.5**.
**ОБЩАЯ 7.1** (взвешено по 8; лёгкий минус — новые оси вскрыли отказоустойчивость и мёртвый вес, невидимые 4-осевому замеру; инфляции нет).

### Новые находки
- ◐ **N-16 [resilience/P1] Единая MTProto-сессия на 3 коннектора без health-check/супервизора** — ✅ ГЛАВНОЕ
  СДЕЛАНО 09.07 (`run_pipeline` health-check): если ПОСЛЕ попытки тяги выгрузка ≥`STALE_ALERT_HOURS`(24ч) или
  её нет → `logging.critical` + строка в отчёт + best-effort уведомление владельцу (`publish.notify`) с указанием
  проверить `data/evgeniyp.session`. Больше НЕ молчит на устаревших данных. Поток не менялся (аддитивно).
  ОСТАЛОСЬ (осознанно, над-инженерия для solo): супервизор/рестарт ботов, разделение read/write MTProto-сессий,
  независимый канал алерта (Bot API — при мёртвой сессии MTProto-уведомление не дойдёт, но лог/отчёт сработают).
- ☑ **N-17 [product/P1] Threads построен (1353 стр), НЕ подключён** — ✅ СДЕЛАНО 09.07 (`fca648b`): помечен
  в ARCHITECTURE §3 «построено vs подключено». Остаток (не срочно): интеграционный тест ПЕРЕД подключением Ф2.3.
- ☑ **N-12** base64 обложки без кэпа размера (`scope_writer.py:239,264`) — ✅ СДЕЛАНО 09.07 (`fca648b`): кэп 4МБ в `_vision_pick`/`_vision_ok`, большой кандидат пропускается с warning (страховка — source_media и так режет).
- ☑ **N-18 [product/P2] Личный ассистент — статус живой/прототип неясен** — ✅ ЗАКРЫТ УДАЛЕНИЕМ 12.07.2026:
  владелец решил не держать ассистента отдельным ботом. Удалены `main.py`/`core/bot.py`/`tools.py`/`memory.py`/
  `agents/personal-assistant/` (восстановимо из истории git). Заодно удалён developer-агент (служебный, не в критпути).
- ☑ **N-19 [data/P2] Файл-шина `drafts/` публикует ЛЮБОЙ .md по mtime** — ✅ СДЕЛАНО 09.07 (`fca648b`):
  `_md_files` берёт только `YYYY-MM-DD-*.md` (так пишет `_save_draft`) — посторонний файл не станет постом и не собьёт гейт свежести.
- ☑ **N-20 [quality/P2] `content_plan.py:19-22` хардкод дней/времени — ручное зеркало post_standard.md** (=хрупкость #3). — ✅ ЗАКРЫТ ранее (sync-комментарий в docstring; парсинг .md признан над-инженерией).
  **Фикс (doc-only):** предупреждение-комментарий «держать в синхроне». Парсинг .md — над-инженерия, не сейчас.
- ☑ **N-21 [security/P2] `analytics.py` subprocess получает весь `os.environ`** — ✅ ЗАКРЫТ КАК ACCEPTED 12.07:
  дочерний процесс наследует окружение ПО УМОЛЧАНИЮ (код строит `env=` лишь чтобы ДОБАВИТЬ UTF-8-флаги —
  экспозиция та же, что без `env=`); это СВОЙ `refresh.py`, которому секреты (telethon API_ID/HASH, ключи enrich)
  легитимно нужны. Белый список опасен: пропуск прокси/сертификата/ключа → тихий отвал сборщика → устаревшая
  аналитика (то, от чего N-16). Выигрыш ~нулевой, риск реальный → не чиним кодом.
- ☑ (= **N-10**) ещё 3 модуля читали JSON без io_safe/лога — ✅ СДЕЛАНО 12.07 (`42ab4a7`): `agent_runtime._read_run_date`
  переведён на `io_safe.load_json`; `creator_tools:312`/`tg_format:31` уже были на io_safe (проверено).
- ☑ **N-22 [ops/P2] `core/test_scope_media.py` в core/ вместо tests/** — ✅ СДЕЛАНО 09.07 (`fca648b`): `git mv` в tests/.
- ☑ (= **P2-9**) история диалога в RAM — ✅ БОЛЬШЕЙ ЧАСТЬЮ МООТ 12.07: главный потребитель (личный ассистент)
  удалён; оставшиеся боты команд-driven / `run_pipeline` one-shot. Персист истории больше не приоритет.
- (= **N-14**) изоляция защищена на ЧТЕНИЕ, не на запись — доп. дёшево: маркеры `[SCOPE/FLAGSHIP/SHARED]` в шапках общих файлов.
- ☑ **N-23 [security/P2] `gpt_image` playwright `eval_on_selector_all` — JS в контексте страницы** — ✅ СДЕЛАНО
  12.07 (`generate.py:_dump_diagnostics`): заменено на locator API (`page.locator("main img").nth(i).get_attribute`) — без выполнения JS-строки на странице.

### 🗑 Мусор на удаление
- `data/result.json` (3.2МБ legacy-экспорт Telegram Desktop, не импортируется, восстановим из git) — засоряет бэкапы
- `data/analyze.mjs` (7КБ разовый Node-скретч, Python-проект на него не ссылается)
- `install.bat` + `start_analyst.bat` (Windows-обвязка для 1 из 5 агентов, несимметрично, dev-convenience) — удалить
- `core/test_scope_media.py` → `tests/` (перемещение, не удаление — двойной discovery)

### Вердикт продукт-фита
Ядро — сильный переиспользуемый актив под профиль (инверсия зависимостей, две ветки на одном ядре, масштаб на
Threads/Twitter без переделки). Мёртвый вес относительно использования: **Threads** (1353 стр, 0 подключений — задел
Ф2). ~~личный ассистент~~ и ~~`agents/publisher/`~~ (ghost-роль) **удалены 12.07.2026** — команда сведена к
трём контентным агентам; публикация живёт в `creator_tools._publish_now` (не тронута). НАД-инженерия (явно в конец): разделение сессий, SQLite-персист,
CI/ruff/mypy, версии схем, pydantic, кэш regex. НЕДО-инженерия (реальная боль, копейки): громкий алерт при разрыве
MTProto (N-16) и base64-кэп (N-12). Итог: механика работает end-to-end, но заявленная функциональность шире
подключённой — нужна честная РАЗМЕТКА СТАТУСОВ, не новый код.

### Рекомендованный порядок (под solo-профиль)
1. **N-12** base64-кэп обложки (срывает публикацию scope; паттерн в source_media). 2. **N-16** health-check+алерт
MTProto (не молчать при разрыве; без разделения сессий). 3. **N-19** фильтр имени `drafts/`. 4. **Мусор:**
`git rm` result.json/analyze.mjs/*.bat + `git mv` test_scope_media. 5. **N-10-доп** io_safe в 3 модуля. 6. **Doc-only:**
статусы Threads/ассистента, синхрон content_plan, маркеры общих файлов. 7. except-детализация scope_writer.
8. тесты verify. **9. ОТЛОЖИТЬ (над-инженерия под solo):** SQLite-персист, systemd/Docker, разделение MTProto-сессий,
CI/pyproject, версии схем, кэш regex — по факту фазы/публичного релиза, не сейчас.

## Полный аудит 2026-07-15 (8 осей) — после Threads-конвейера (ветка feat/threads-content-pipeline)

Workflow `full-audit` (9 агентов). Общая **7.1 → 7.4**: качество кода 7.5→8 (осознанный fail-open/fail-closed,
io_safe на горячем пути), инж-зрелость 6→6.5 (CI зелёный, 135 тестов, наблюдаемость `_guard`), данные 7.2→7.5,
продукт-фит 6.5→7.5 (Threads из «1353 строк с 0 подключений» стал рабочей фичей — N-17 закрыт практикой;
обрезка 12.07 выполнена). Отказоустойчивость 6.0→6.5 (алерт N-16 работает, но идёт через ту же MTProto).
Ни одного нового P0/P1. Сквозная тема четырёх осей: ДОКУМЕНТАЦИЯ ОТСТАЛА ОТ КОДА — для non-coder-владельца
карта правды была недостоверна; чинится дешевле всего в списке. `_guard.py` отмечен как образцовая защита
внешнего аккаунта (урок 14.07 конвертирован в код).

### Новые находки (все P2; закрыты 15.07 в день аудита, кроме явно отложенных)
- ☑ **N-24 [docs] ARCHITECTURE.md устарел и противоречил сам себе** (сводная находка 4 осей): «5 ботов»
  вместо 3, «ни один core/* не импортирует connectors.threads» — опровергнуто `analyst_tools.py:12`,
  вся Threads-ветка (мини-флагман/журнал/память) отсутствовала. ✅ СДЕЛАНО 15.07: doc-проход — шапка,
  §3 (+run_threads_pipeline/refresh*), «построено vs подключено», §4 (threads_creator+flagship_journal+
  io_safe/tg_scoring/untrusted), §4.1 (таблица на 3 ветки), §6 (threads-память и data), §9 (+2 хрупкости).
- ☑ **N-25 [quality] `auth.valid_token()` глушил провал refresh Threads-токена молча** (`except: pass`,
  ни одного лога) — деградация была бы невидима до жёсткого «ИСТЁК» на 60-й день. ✅ СДЕЛАНО 15.07:
  warning с остатком дней; поведение не менялось.
- ☑ **N-26 [product] `run_threads_pipeline` обещал «тестовый канал», слал в боевой `PUBLISH_CHANNEL`**
  и занимал боевой слот отложки — риск случайно выпустить обкатку в эфир. ✅ СДЕЛАНО 15.07: опциональный
  `THREADS_TEST_CHANNEL`; fallback на боевой — с громким предупреждением; докстроки честные.
- ☑ **N-27 [data] `backup.py` дрейфовал от факта `data/`**: незаменимые `published_flagships.jsonl`
  (вход Threads-дистиллятора!), корпус голоса (4966 реплик) и психотип-выжимка были ВНЕ классификации.
  ✅ СДЕЛАНО 15.07: → IRREPLACEABLE; scout_seen/creator_pending_media → SCRATCH; строки в OPERATIONS.md.
- ☑ **N-28 [arch] Изоляция памяти третьей ветки жила «на дисциплине»** (страж-тест знал только
  scope↔флагман). ✅ СДЕЛАНО 15.07: THREADS_ONLY-набор + взаимные проверки трёх веток + анти-vacuous
  страж в `test_isolation_scope_flagship.py`.
- ☑ **N-29 [ops] Ops-поверхность Threads (стоп-кран/cooldown/журнал) без рунбука** — владелец упёрся бы
  в закрытую по умолчанию сеть без инструкции. ✅ СДЕЛАНО 15.07: раздел «🧵 Threads» в OPERATIONS.md
  (unlock, бюджеты, предохранитель, чек «почему молчит»).
- ☑ **N-30 [quality] `threads_dedup._load` — голый json.loads мимо io_safe** (регресс класса N-10).
  ✅ СДЕЛАНО 15.07: io_safe.load_json. (Модуль — задел Формата 2, в проде пока не вызывается.)
- ◐ **N-31 [security] Нет CVE-скана зависимостей; транзитивный js2py 0.74 (CVE-2024-28397,
  sandbox-escape→RCE) в бою у Скаута через twikit.** ✅ ЧАСТИЧНО 15.07: pip-audit в dev-зависимости +
  non-blocking шаг CI (жёлтая отметка = смотреть лог). ОСТАТОК владельцу: проверить, выкинул ли свежий
  twikit js2py целиком (у нас twikit пропатчен — слепой апгрейд нельзя, только осознанный).
- ☑ **N-32 [ops] Тест-файлы вне tests/ (недоведённый N-22)**: `connectors/source_media/test_fetch.py`.
  ✅ СДЕЛАНО 15.07: → `tests/test_source_media_fetch.py`. (`gpt_image/test_render.py` — смоук-скрипт,
  переименование = над-инженерия, оставлен.)
- ☑ **N-33 [docs] Битая ссылка в шапке `_guard.py`** (docs/ вместо notes/). ✅ СДЕЛАНО 15.07.
- ☐ **N-34 [security] SSRF-фильтр уязвим к DNS-rebinding** (resolve-then-connect TOCTOU в feeds.py) —
  **ЯВНО НАД-ИНЖЕНЕРИЯ под solo/бёрнеры**: зафиксирован как известный лимит, чинить перед публичным
  релизом (резолв один раз + коннект по IP).
- ☐ **N-35 [product] `threads_dedup` + `orientation_digest` построены, никем не вызываются** — осознанный
  задел Формата 2: НЕ удалять и НЕ достраивать вперёд нужды (разметка в ARCHITECTURE «построено vs подключено»).
- ☐ **N-36 [quality] `run_pipeline._threaded` — новый ThreadPoolExecutor на каждый LLM-вызов** — копеечные
  накладные на фоне многосекундных вызовов; только попутно, отдельно не делать.

### Сильные стороны, подтверждённые 15.07 (не регрессировать)
Инверсия зависимостей доказана ДЕЛОМ (третья ветка без правки ядра); изоляция веток под статическим тестом;
безопасность целостна (0 хардкод-секретов, OWNER-гейт, двухслойная анти-инъекция, 0 eval/pickle/shell=True);
`_guard.py` — образцовая защита аккаунта (сеть закрыта по умолчанию, бюджеты, x-app-usage, журнал);
бюджет-дисциплина в коде (тиры, кэпы, cost_log + крик при кэш-хите <40%); осознанный fail-open/fail-closed
(dedup закрыт, verify/media открыты, publish не бросает); CI честный (135 детерминированных тестов).

### Осознанно отложено (триггеры, не даты)
Автопилот → дубль stale-алерта через Bot API (~10 строк), супервизор/хартбит, разделение MTProto-сессий.
Публичный релиз → N-34 DNS-rebinding. Формат 2 → машинерия threads_dedup/orientation_digest. Не сейчас → N-36.

## Полный аудит 2026-07-17 (8 осей) — после замыкания ТГ-контура само-обучения

Оси: Архитектура 8 · Безопасность 8 · Качество 7.5 · Инж-зрелость 6.5 · Мусор 7.5 ·
Отказоустойчивость 6.5 · Данные 7.5 · Продукт-фит 7 · **Общая 7.2**. Багов в критпути нет
(все P2 + два отложенных P1). Здоров и реально используется ($91/4нед, 152 драфта).

**Починено В ТОТ ЖЕ ДЕНЬ (наблюдаемость + карта правды — цель владельца «навести порядок»):**
- ☑ **N-37 (P2) Doc-drift** — карта врала: петля подключена 17.07, а ARCHITECTURE §4.2/README/
  self_learn_check писали «дормант/не подключено» + веса «0.65/0.35» (боевой путь TG-only). *(doc-проход, коммит E)*
- ☑ **N-41 (P2) Голый json.loads на живом пути пикера** (регресс N-10) — `self_learn.py`, `report.py`
  → `io_safe.load_json`. *(коммит A)*
- ☑ **N-42 (P2) `_bank_map` перечитывал банк ~200×/прогон пикера** (регресс N-15) — кэш по (путь,mtime). *(коммит B)*
- ☑ **N-43 (P2) broad-except без лога** в §4.2-модулях (регресс N-11) — логи в `self_learn`/`topic_category`/
  `threads_creator`; главное — молча выключенный наклон пикера теперь ЛОГИРУЕТСЯ. *(коммиты A,B)*
- ☑ **N-44 (P2) Тихое выключение наклона при рассинхроне нормализации** dedup↔topic_category — тест-страж. *(коммит 5)*
- ☑ **N-39 (P2) Backup-дрейф** (рецидив N-27) — `threads_distillations.jsonl` в IRREPLACEABLE +
  print_lists сканирует data/ и предупреждает о неклассифицированном + минус Chromium-кэш gpt_profile (~264МБ). *(коммит D)*

**Требует владельца (питон у него):**
- ☐ **N-40 (P2) requirements.lock рассинхрон** — Pillow выпал из лока, CI тестит НЕ тот набор. Фикс:
  `pip freeze > requirements.lock` на рабочей venv.

**Заморожено — над-инженерия под solo (НЕ достраивать, валидировать на владельце):**
- ❄ **N-38 (P1→заморожено) Threads-половина self-learn на 0 датапоинтов** — `threads_best=None` в проде,
  блендинг 0.65 не срабатывает; `topic_datapoints`/`threads_distill_link`/`text_match`/`threads_flagship_link`
  потребляются только `self_learn_check`. НЕ выпиливать (изолировано, fail-open). Разморозка — при реальных
  `threads_distillations`. Переразмечено в §4.2 как дормант.
- ❄ **N-47 (P2) Бандит explore/усадка/рецентность** ради ~2 флагманов/нед по 6 категориям, а финал всё
  равно переигрывает LLM — enterprise-плюмбинг. Оставить как есть (fail-open), не расширять.

**Сделано 18.07 (продолжение уборки):**
- ☑ **N-45 (дешёвая половина) Health-алерт через Bot API** — раньше алерт «MTProto мертва» шёл через
  ТУ ЖЕ мёртвую сессию → не доходил. `core/bot_alert.notify_owner` (токен бота+HTTPS, независимо от
  MTProto). *(коммит N-45)*. Полное разделение сессий (export/scan/publish) — ☐ по триггеру автопилот/релиз.
- ☑ **N-49 Дубль enrich_topics сведён** — единый `connectors/enrich_common`, обе обёртки тонкие,
  загрузка через io_safe (устранён guard-дрейф). Правка промпта/логики теперь в одном месте. *(коммит N-49)*

**Решения владельца / accepted-риск:**
- ✅ **N-48 (P2) 2FA-фактчек — РЕШЕНО: оставить** (владелец 18.07). Цифры недели: verify $4.88 (24%,
  #2 после scope), гоняется в основном на SCOPE — а это новости/атрибуции, где фактчек и отрабатывает
  (баг с выдуманным сенатором ловился ровно им). ~$21/мес — осознанная страховка доверия, не дефект.
- ☐ **N-46 (P2, accepted) js2py RCE (CVE-2024-28397 через twikit)** — X = read-only бёрнер по кукам, не
  парольный вход → challenge-путь js2py вероятно не на горячем; триггер пересмотра — публичный релиз/автопилот X.

**Что НЕ удалять:** `threads_dedup`/`orientation_digest` (осознанный задел Формата 2, N-34/N-35);
кросс-платформенная половина self-learn (изолирована, fail-open). Осиротевших модулей в проекте нет.

---

## Changelog аудита
- **2026-07-18** — Продолжение уборки по панч-листу 17.07: ☑ N-45 (health-алерт через Bot API, не через
  мёртвую MTProto), ☑ N-49 (сведён дубль enrich_topics в connectors/enrich_common). N-48 РЕШЕНО владельцем
  по цифрам недели — 2FA оставить (отрабатывает на scope-новостях, $21/мес осознанная страховка).
  Ruff сделан блокирующим (5 находок F401/F541 вычищены), лок перекодирован UTF-8 (N-40).
- **2026-07-17** — Полный аудит 8 осей (9 агентов, 646К токенов) после замыкания ТГ-контура обучения.
  Общая 7.4→7.2 (флэт). Багов в критпути нет. В ТОТ ЖЕ ДЕНЬ закрыты N-37 (doc-drift: карта врала о
  подключённости), N-39 (backup-дрейф), N-41/42/43 (регрессы N-10/11/15 на живом пикере: io_safe,
  кэш банка, логи в except), N-44 (страж рассинхрона нормализации). Заморожены N-38/N-47 (Threads-
  половина + бандит = построено-впрок под solo). Отложены по триггеру N-45/46. Owner-долг N-40 (lock).
- **2026-06-17** — Первый полный аудит (4 оси, 4 параллельных ревьюера). Общая 6.5/10. Заведён этот файл,
  панч-лист P0–P2 (15 пунктов). Все статусы — открыты; реализация по согласованию с владельцем.
- **2026-06-23 (P0-фиксы)** — Закрыты P0-1 (авторизация OWNER_ID), P0-2 (try/except), P0-3 (баг токена).
  Удалены тест-скрипты публикации. Общая 6.5→7 (коммит `760aba4`).
- **2026-06-23 (итог дня)** — Закрыты: P0-16 (секрет вычищен из истории git + force-push), P2-11 (SSRF-фильтр),
  P1-4 (lock+потолок), N-1 (.python-version), N-7 (docstring), P2-10 (ARCHITECTURE.md). Добавлены: тесты
  (32 кейса, P1-5), фикс кодировки requirements под RU Windows. Общая 7→7.5. Отложено (P2): реестр tool-схем
  (P2-8/13), logging_setup (N-2), persist истории (P2-9), CI (P2-12), Docker/бэкапы (P1-6/P2-14).
- **2026-06-23 (переаудит)** — Повторный прогон 4 ревьюеров (цель: порядок до роста). Новый P0-16
  (захардкоженный api_hash в истории git) → безопасность 8→7. Новые находки N-1…N-8, P2-8/13/9 подняты до
  P1, добавлен раздел «рекомендованный порядок» и фиксация карты связей для онбординг-документации.
- **2026-07-09** — Сессия удешевления scope-вектора + гигиена. Скаут −44% ($2.02→$1.14/прогон): бюджет-
  мышление вместо adaptive (`llm.resolve_thinking`) + web_search 8→4. Фиксы: paused-гейт по границе слова
  (был substring-баг «мика»→«экономика», холостые генерации), честная дока `verify` (Sonnet by design).
  Доки: карта веток `ARCHITECTURE §4.1`, `agents/scout/README` (стоимость), `OPERATIONS.md` (бэкап N-5).
  Тесты: +`_hits_paused`/+`resolve_thinking` (P1-5). Затем ПЕРЕАУДИТ (workflow, 4 оси + синтез): общая
  **7.5→7.3** (калиброванный минус — тесты/ops-долг обогнали рост кода); новые находки N-9…N-15, регрессии
  и рекомендованный порядок — в разделе «Переаудит 2026-07-09» выше.
- **2026-07-09 (полный аудит, 8 осей)** — Workflow `full-audit` (8 ревизоров: +мусор/отказоустойчивость/данные/
  продукт-фит). Общая 7.3→**7.1** (новые оси вскрыли отказоустойчивость 6.0 и мёртвый вес — Threads/ассистент/
  publisher построены-не-подключены). Находки N-16…N-23 + список мусора + вердикт продукт-фита. Топ-3 дешёвой боли:
  base64-кэп обложки (N-12), health-check MTProto (N-16), фильтр имени файла-шины (N-19). Скрипт многоразовый в `.claude/workflows/`.
- **2026-07-09 (фиксы после полного аудита, `fca648b`)** — Закрыты дёшево: N-12 (base64-кэп обложки),
  N-19 (фильтр имени драфтов), N-22 (test в tests/), N-17/N-18 (разметка «построено vs подключено» в
  ARCHITECTURE §3), N-10-доп (io_safe в custom_emoji/emoji_map). Удалён мусор (install.bat/start_analyst.bat;
  test_scope_media→tests). N-20 уже был закрыт (sync-комментарий в content_plan docstring). ОСТАЛОСЬ (боевой
  путь, отдельным аккуратным заходом): **N-16** (громкий алерт при разрыве MTProto). Прочее (N-21/N-23,
  тесты verify) — по мере надобности. data/result.json+analyze.mjs (не в git) — чистит владелец у себя.
- **2026-07-09 (N-16 health-check)** — `run_pipeline` теперь громко сигналит, если выгрузка канала не
  обновляется (≥24ч/нет) после попытки тяги: `logging.critical` + отчёт + best-effort уведомление владельцу
  (проверь MTProto-сессию). Единая точка отказа больше не тихая. Супервизор/разделение сессий/Bot-API-алерт —
  осознанно отложены (над-инженерия для solo). Закрыт последний дешёвый P1 полного аудита.
- **2026-07-12 (фокус на завод + зелёный блок)** — Стратегическое решение владельца: держать только
  контент-завод (Скаут/Криейтор/Аналитик). Удалены как отдельные боты **личный ассистент** (`8854438`) и
  **developer** (`0436dfd`), а также **publisher-роль-оболочка** (`ab81e26`) — руки публикации
  (`_publish_now`+`telegram_publish`) сохранены. Закрыты дешёвые находки (`42ab4a7`): **N-15** (кэш regex
  в dedup), **N-10-доп** (io_safe в `_read_run_date`), **N-8** (chat владельца пишется только allowed),
  **N-2** (единый `logging_setup`). Доки синхронизированы (README/ARCHITECTURE/CLAUDE, `4c011ca`). Закрыты
  удалением: **N-18** (ассистент), publisher-ghost-роль; **P2-9** (RAM-история) — большей частью моот.
  N-4 (.bat) уже был вычищен ранее. Затем добит остаток находок: **N-11** (разнесены except API/IO/прочее в
  vision `scope_writer`), **N-23** (playwright locator API вместо JS-eval в `gpt_image`), **N-21** — закрыт
  как ACCEPTED (свой subprocess наследует env по умолчанию; урезание опасно, выигрыш нулевой). N-20 был закрыт
  ранее. **Итог: весь панч-лист находок N-* и P1/P2 закрыт либо осознанно принят;** открытым остаётся только
  задел Threads (Ф2, не находка) и над-инженерия, явно отложенная под solo-профиль.
  Тесты владельца после сессии: **87 зелёных**. Осиротевшие `SECRETARY_BOT_TOKEN`/`DEVELOPER_BOT_TOKEN` в
  `.env`/`.env.example` владелец решил ОСТАВИТЬ намеренно (резерв на возможное возвращение ботов; токены не
  меняются) — не мусор, не флагать к удалению.
- **2026-07-12 (ред-блок — инфра-долг)** — После закрытия находок взяли структурный «red-блок». **P2-12**
  (`fd117a7`): GitHub Actions гоняет `pytest` на push/PR (зелёная галочка = страховка для non-coder; CI зелёный
  с первого прогона). **P2-15** (`810ba98`): наблюдаемость — `logging_setup` инъектит `[agent/req]` в каждую
  строку (contextvars + хендлер-фильтр). **P2-14 + N-3** (`df727ca`): `backup.py` — бэкап незаменимого одной
  командой + классификация «живое/скретч» в коде (единый источник правды). **N-6**: двухслойная защита от
  prompt-injection — обёртка чтения (была) + страж `reject_if_framed` на долгоживущих записях. Новые тесты:
  `test_logging_setup`/`test_backup`/`test_untrusted`. ОСТАЛОСЬ в red: только **P1-6** (Docker/автозапуск —
  осознанно последним, завязан на хартбит) и частичный **P1-7** (обзор `.md`/`read_text`-парсинга).
- **2026-07-15 (полный аудит, 8 осей + фиксы в тот же день)** — Workflow `full-audit` (9 агентов) после
  Threads-конвейера. Общая 7.1→**7.4**, ни одного нового P0/P1. Находки N-24…N-36; сквозная тема — доки
  отстали от кода. В ту же сессию закрыто: N-24 (doc-проход ARCHITECTURE: 3 бота, Threads подключён,
  третья ветка в §3/§4/§4.1/§6/§9), N-25 (warning на молчащий refresh токена), N-26 (THREADS_TEST_CHANNEL
  вместо молчаливого боевого), N-27 (backup.py: журнал флагманов/корпус голоса/психотип → IRREPLACEABLE),
  N-28 (THREADS_ONLY в тесте изоляции), N-29 (рунбук стоп-крана Threads в OPERATIONS.md), N-30 (io_safe в
  threads_dedup), N-32 (test_fetch→tests/), N-33 (ссылка _guard). N-31 частично (pip-audit non-blocking в CI;
  остаток — осознанный апгрейд twikit). Явно отложены: N-34 (DNS-rebinding — до публичного релиза),
  N-35 (задел Формата 2 — не трогать), N-36 (микро). Мусор удалён: result.json/analyze.mjs/пустые огрызки
  корпуса/memory-остатки ассистента/docs/plan_old.md. Заодно закоммичен хвост 15.07: честный UA вместо
  подставного Chrome + снят «ночной режим» (маскировка под человека ≠ защита аккаунта) + добор метрик-долгов.

## Аудит по функциям 2026-07-20 — гейты scope, уроки, линтер, Скаут (инкремент)

Сессия под «улучшаем, не ломаем, объективно». Метод — как с линтером: читаем функцию, ищем
задокументированный/латентный вред, чиним только провабли-безопасное (тестами), рискованное выносим
отдельно. Всё аддитивно, happy-path не тронут. Приоритет аудита выбран по сигналу: покрытие тестами ×
центральность × свежесть переработки.

### Scope (гейты качества)
- ☑ **N-50 [product/P1] scope ставил в отложку слабый/несвежий повод.** ✅ 20.07 (641e891 + тесты ca0c7a1) —
  мягкий гейт свежести (протухающая новость vs долговечное развитие) + жёсткий гейт **польза-эдж**
  (`core/usefulness`): пересказ без вывода через неочевидный угол → СТОП. «Нет пользы — дата не спасёт».

### Уроки (обрастание жиром — бьёт по цене КАЖДОГО поста)
- ☑ **N-51 [cost/P1] Файлы уроков append-only, ~20% контекста Opus-флагмана.** ✅ 20.07 (2cc8733/5d9c8ae/
  3fb4f29) — стрип провенанс-хвостов при загрузке (−24% post_lessons), страж дублей мануала, lifecycle
  секции «Выпущено» (правило в линтере/мануале выходит из оплаты), порог-WARN. Ни одно ПРАВИЛО не удалено.

### Линтер (`_lint` — самый дешёвый дом правил)
- ☑ **N-52 [quality/P1] Детектор антитезы ПОРТИЛ подачу.** ✅ 20.07 (5dcc42f) — обратная форма «X, а не Y»
  ловила простой контраст → модель клипала «а не»→«не» → телеграфный тон хуже (урок :33). Считаем только
  прямую. +10 тестов на нечёткие детекторы (были без покрытия). Намеренно НЕ трогали: суперлатив
  «крупнейший» (флажит по делу, урок :16), стаккато ≤3 слов (расширение = ложняки), авто-болд (покрыт).

### Скаут (капремонт 13.07 — центральный, был почти без тестов)
- ☑ **N-53 [cost/P1] Воронка не возвращала cost-контекст.** ✅ 20.07 (cf38f48) — `scout_funnel.sift` ставил
  `set_context("funnel")` и не восстанавливал → турны Скаута-Sonnet после билялись как funnel (искажал
  cost-анализ). save/restore через finally + `cost.get_context()`.
- ☑ **N-54 [resilience/P1] Тихая деградация Скаута → «тихий день».** ✅ 20.07 (3030021) — (а) login-wall X
  (пусто без ошибок) → явная ошибка-запись (`x_scan/read.recent`); (б) баннер «источник недоступен» стал
  СТРУКТУРНЫМ сигналом (`_degraded_sources`) + WARNING в `_run_scout` — не только текст, который LLM мог не донести.
- ☑ **N-55 [ops/P1] Машинерия капремонта 13.07 без тестов.** ✅ 20.07 (cf38f48+3030021) — 18 тестов
  `scout_funnel`/`scout_seen`/баннер/login-wall (parse/sift/filter_new/commit/SEEN_CAP).
- ☐ **N-56 [resilience/P2] Нет гейта свежести по ДАТЕ в коннекторах Скаута.** ОТЛОЖЕНО (дизайн) — редко
  обновляемый фид отдаёт месячную-но-невиданную запись как свежую; фикс требует парсинга разнобойных
  форматов дат (новая поверхность багов). Наблюдения (P2, не трогаем): SEEN_CAP-эвикция недетерминирована
  (`_pending`=set); verbatim-exception-строки в контекст LLM; funnel bills на дефолтный ключ, не scout-ключ.

### verify / 2FA (красная линия — цифры/атрибуция, максимальная цена ошибки)
- ☑ **N-57 [quality/P0] Авто-пайплайн НЕ блокировал остаточный ⚠️ 2FA.** ✅ 20.07 (9d4ff63) — в
  run_pipeline (флагман) и scope_writer ⚠️ был лишь совещательным: одна авто-правка → публикация БЕЗ
  перепроверки; у флагмана результат ре-верификации даже вычислялся, но ВЫБРАСЫВАЛСЯ → остаточный ⚠️
  (правка не помогла) уходил в отложку. Стопил только /schedule. Фикс: ре-верификация флагмана теперь
  проверяется → остался ⚠️ → СТОП+уведомление. (Blast radius был мал: боевой канал ручной, PUBLISH — тест.)
- ☑ **N-58 [quality/P1] Атрибуционный регекс (фикс 15.07) — частые пропуски.** ✅ 20.07 (9d4ff63) —
  `find_attributions` терял: двоеточие-цитату «Имя: «…»», «по данным/информации/версии X», «согласно X»,
  глаголы предупредил/рассказал/настаивает/полагает/допустил/опасается/предрекает. Каждый пропуск = потеря
  код-нуджа на имя → падало на «модель, может, заметит» (та же дыра, что пропустила Ламмис). Регекс расширен
  (аддитивно, ложняки дёшевы — модель сверяет). OWNER_POST по-прежнему []. +red-line тесты (фейл-открыто).
- ☐ **N-59 [quality/P1] scope-ветка 2FA без ре-гейта; фейл-закрыто на КРАШе.** ОТЛОЖЕНО — (а) scope_writer
  делает одну правку и `return post` без перепроверки (блок требует изменить контракт возврата → плумбинг);
  (б) все режимы сбоя 2FA фейл-ОТКРЫТО (краш/пусто → публикует) — для red-line спорно, но боевой канал
  ручной, а инфра-флейк не должен блокировать. Оба — решение владельца отдельно.

### analytics (фундамент данных — на нём стоят дедуп/скоринг)
Узкая проверка целостности (владелец считал модуль хорошим — читатели через io_safe и правда крепки).
Дыра в ПИСАТЕЛЕ: Threads-сборщик защищён (_merge_keep), TG-сборщик — нет.
- ☑ **N-60 [data/P0] TG-сборщик мог тихо затереть корпус урезанной/пустой выгрузкой.** ✅ 20.07 (3dc62ba) —
  `telegram_export/collect` делал безусловную полную перезапись `channel_posts.json`; транзиентный сбой
  Telegram (меньше/пусто на exit 0) молча терял историю, а mtime-гейт свежести (`dedup.data_age_hours`)
  сертифицировал пустоту как «свежо» → дедуп видит пустую историю → дубли проходят, скоринг отравлен.
  Фикс `_safe_write`: пусто/<50% существующего → не пишем (старые данные + честный mtime целы), иначе
  атомарно (temp+os.replace). +5 тестов. Зеркалит защиту Threads.
- ☐ **N-61 [data/P2] all-null-views + mtime-свежесть по контенту.** ОТЛОЖЕНО — сервер может отдать посты
  с views=None у всех → `_load_posts` даёт [] → та же ложно-зелёная свежесть; и `data_age_hours` меряет
  mtime, не контент. N-60 закрывает главный путь (пусто/усечение); этот подслучай реже, а его гейт (views
  не None) рискует ложняком на свежих постах — отдельным решением.

### self_learn (петля скоринг→генерация) — ЗДОРОВА (в отличие от аналитики)
Вердикт: петля РЕАЛЬНО замкнута и потребляется (не логгируется-впустую). Сигнал доходит до выбора темы
(`run_pipeline.py:189-191`: `weighted_sample(pool, …, cat_w.get(category_of(t)))`) — веса категорий
смещают, какие 25 из ~134 тем банка попадут к LLM. Защита от no-op крепкая (видимый WARNING на сбое,
`test_bank_norm_consistency` против дрейфа ключей, равномерно-не-сломано на пустых данных) и от вреда
(клэмп весов [0.6,1.4], усадка по уверенности, explore-пол против голодания, финал за LLM).
- ☑ **N-62 [ops/P2] Тихий no-op наклона при пуле ≤25.** ✅ 20.07 — `min(25,len(pool))`: если банк усохнет
  метками `[вышло]` до ≤25 тем, `weighted_sample` отдаёт ВСЕ → наклон не влияет, молча. Сейчас пул ~134
  (норма), но добавлен WARNING, чтобы вырождение стало видимым.
- ☐ **N-63 [product/P2] Влияние петли МЯГКОЕ + замыкание без интеграционного теста.** ОТМЕЧЕНО (не баг):
  (а) веса смещают лишь ПОПАДАНИЕ в 25 кандидатов, финальный выбор за свежестью/рынком/LLM — эффект на
  выход умеренный ПО ДИЗАЙНУ (это ответ на вопрос владельца «влияет ли»: да, но мягко); (б) части петли
  покрыты юнит-тестами, но НЕТ теста, что `_pick_timely_theme` целиком даёт дифференцированную выборку
  (сильная категория чаще). Интеграционный тест (мок LLM/данных, счёт частот) закрыл бы — отдельно.

### Флагман-движок — только МЕХАНИКА (подачу/голос НЕ трогали — golden)
Владелец: «проверь, но выдели отдельно, чтобы не сломать где не надо». Аудит поделён жёстко: 🟢 сантехника
(ошибки/данные/стоимость/покрытие — можно чинить) vs 🔴 голос/промпт (что-как пишет — DO NOT TOUCH, откаты
30.06/13.07 доказали, что над-инженерия промпта ломает качество). Линтер аудирован отдельно (N-52).
- ☑ **N-64 [quality/P0] 2FA-регейт верифицировал ПАМЯТЬ, публикуется ДИСК.** ✅ 20.07 (529dd08) — ре-верифи
  шёл по `post` в памяти, а `publish_now` читает драфт с диска; фикс-модель могла поправить текст, но не
  сохранить → в канал уходил старый драфт с ⚠️-цифрой (мой же N-57 это НЕ закрывал!). Теперь верифи по
  `verify.latest_draft()` — что реально публикуется. Красная линия закрыта на артефакте.
- ☑ **N-65 [ops/P2] LAST_KIND глотал сбой записи (`except: pass`).** ✅ 20.07 (529dd08) — тихий сбой →
  publish_now гадает формат по длине → флагман под порогом становится 'short' и теряет обложку. Теперь WARNING.
- ☐ **N-66 [data/P1] Обложка теряется в ИНТЕРАКТИВНОМ `/schedule` после 2FA-правки.** ОТЛОЖЕНО (механика,
  но деликатный publish-путь) — 2-секундный mtime-гейт обложки: `/post` пишет LAST_COVER в T1, авто-2FA-фикс
  пересохраняет драфт в T2 (T2−T1 = целый LLM-вызов) → `T1+2 ≥ T2` ложно → обложка молча снята, флагман
  уходит текстом. Пайплайн защищён forced-cover, бот — нет. Фикс = стабильная привязка обложка↔драфт (по слагу).
- ☐ **N-67 [cost/P2] 2FA-фикс грузит ВЕСЬ флагман-контекст ради правки 2 чисел.** ОТЛОЖЕНО (граничит с
  качеством фикса) — `_fix_facts` пересобирает `_system()` (мануал/эталоны/банк) на дорогой модели для точечной
  правки. Облегчить контекст фикса рискует качеством правки → трогать осторожно, отдельным решением.
- ☑ **N-68 [ops/P1] Детерминированный write-path без тестов.** ✅ 20.07 (d6ea1ba) — 8 тестов
  (tests/test_publish_now.py, сеть/планировщик замоканы): `_md_files` (только датированные, свежие сверху,
  гейт N-19); `_publish_now` — срез [[SPLIT]]-меты и [ПРОВЕРИТЬ]-строк (в канал только тело), гварды
  пусто/нет-канала/нет-драфта, forced-обложка минует гейт, флагман без валидной обложки → ТЕКСТОМ.
  ОСТАЁТСЯ (с N-66): stale-LAST_COVER mtime-гейт (нужна utime-гимнастика).
- 🔴 **Голос — граница DO NOT TOUCH:** `creator_bot.COMMANDS["post"]`, `FIX_FACTS`, `_anchors`/`_system` ctx,
  `_pick_timely_theme`-промпты, memory-файлы (content_manual/brand/post_standard/format_playbook/post_lessons/
  anchor_posts/image_prompt). Механические фиксы держатся В СТОРОНЕ от них.

### Карта покрытия свипа (что пройдено, что НЕТ — честно)
Свип 20.07 закрыл 7 ПРИОРИТЕТНЫХ узлов (по сигналу: без тестов × центральный × свежесть × цена ошибки).
Это НЕ «весь завод» — остальное осознанно отложено. Кандидаты след. раунда помечены ⏳.

**✅ Пройдено адверсариально (8):** scope · уроки · линтер · Скаут · verify/2FA · analytics · self_learn ·
флагман-МЕХАНИКА. Найдено/закрыто N-50…N-65, отложены с обоснованием N-56/59/61/63/66/67/68.

**⛔ Исключено по решению владельца:** флагман-ГОЛОС/подача (golden, DO NOT TOUCH — но механика пройдена,
см. выше N-64…68) · Threads-подсистема (заморожена до данных, N-38).

**🟢 Зрелые / низкий приоритет (тесты есть, недавно аудированы):** dedup (косметика ИТОГ-панели ждёт go) ·
untrusted (N-6) · market_price · publish/telegram_publish · cost (аудит 15.07 + get_context 20.07).

**⏳ НЕ смотрели вовсе — кандидаты след. раунда:** обложки — `source_media` (og:image+vision, «боль №2»
остаток 13.07) и `make_image` (GPT-браузер, хрупкий внешний узел) · `content_plan` (слоты) · оркестратор
`run_pipeline` (порядок гейтов — интеграционный клей, юнит-тестом не берётся) · выделенный проход
`tg_scoring` (в self_learn прочитан, звучит, но people_count/коммит из памяти открыты).

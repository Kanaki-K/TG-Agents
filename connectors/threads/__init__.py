"""Threads-коннектор (официальный Meta Threads API) — «руки» фазы 2.

Изолирован от флагмана/скоупа: свой хост, свои токены, своя таблица метрик.
Состав:
- _api.py     — общий HTTP-слой к graph.threads.net (stdlib urllib, фикс. хост).
- auth.py     — жизненный цикл токена: разовый OAuth-бутстрап + авто-refresh (60 дней).
- read.py     — свои посты (текст+даты) для анализа контента.
- insights.py — метрики постов и аккаунта → нормализация в общий вид.

Аналитика (веха 2.2, зеркалит telegram_export — таблица раздельная):
- collect.py      — посты + метрики + флаги (медиа/ссылка/хештег) + сводка аккаунта.
- replies.py      — комменты под постами: отделяет ЖИВОЙ отклик людей от self-тредов.
- enrich_topics.py — заголовок/тема/суть через Claude → data/threads_topics.json.
- scoring.py       — единая жёсткая логика: производные метрики + тир + Сила (виральность по РАЗНЫМ людям и распространению, а не по сырым реплаям).
- build_table.py  — итоговая таблица → data/threads_analytics.xlsx / .csv (та же структура, что ТГ).
- report.py       — сухой аналитический отчёт (топ по Силе, ложная виральность, медиа/темы/тайминг).

Публикация (publish.py) + своя отложка — отдельная веха 2.3.
"""

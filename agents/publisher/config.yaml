# «Публикатор» — НЕ отдельный бот, а функция /schedule внутри Криейтора + коннектор
# connectors/telegram_publish. Этот config оставлен как документация роли (см. SKILL.md);
# рантайма/токена/ключа у Публикатора нет (отдельный бот удалён за ненадобностью).
#
# Публикует ДЕТЕРМИНИРОВАННО (без LLM), ФАЙЛОМ (фото+текст, без URL/превью), отложенно, по MTProto
# аккаунтом-публикатором (TELEGRAM_SESSION / data/evgeniyp.session — тот же, что у аналитики).
# Куда — PUBLISH_CHANNEL (инвайт-ссылка). Слоты — core.content_plan (PUBLISH_TZ, PUBLISH_*_TIME).
# Уведомление владельцу — PUBLISH_NOTIFY. Запуск всей цепи — run_pipeline.py.
name: Публикатор
